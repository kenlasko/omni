cluster:
  allowSchedulingOnControlPlanes: true
  extraManifests:
  - https://raw.githubusercontent.com/alex1989hu/kubelet-serving-cert-approver/main/deploy/standalone-install.yaml
  apiServer:
    certSANs:
    - kube-laptop.ucdialplans.com  # This is for a Kube API backdoor that doesn't require Omni to access. Not strictly required. 
    - kube-laptop.laskonet.com  # This is for a Kube API backdoor that doesn't require Omni to access. Not strictly required.