cluster:
  allowSchedulingOnControlPlanes: false
  extraManifests:
  - https://raw.githubusercontent.com/alex1989hu/kubelet-serving-cert-approver/main/deploy/standalone-install.yaml  # For certificate rotation required by metrics server
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.0/experimental-install.yaml               # For Cilium gateway
  apiServer:
    certSANs:
    - kube.laskonet.com  # This is for a Kube API backdoor that doesn't require Omni to access. Not strictly required.